# Loren v0.1 — Trustworthy Core Implementation Plan

**Status:** Active — M5 Action/Credential Boundary + Narrow GitHub Writes  
**Parent plan:** `docs/plans/master-plan.md`  
**Architecture boundary:** ADR-001  
**Accepted stack:** ADR-002  
**Canonical state/memory lifecycle:** ADR-003  
**Action/approval/credential boundary:** ADR-004

v0.1 is the first version where Loren must prove identity, memory, action authorization, credentials, audit, and external tool use can work together safely enough to become a real daily-use foundation.

---

# 1. v0.1 objective

Required flows:

```text
1. "Loren, repo wedding hiện sao rồi?"
   -> resolve project/repository
   -> read current GitHub state
   -> answer from structured tool data

2. "Nhớ rằng project này production deploy phải hỏi tao."
   -> save trusted owner-provided memory/policy
   -> restart Loren
   -> retrieve the same current rule later

3. "Tạo branch và chuẩn bị thay đổi X."
   -> brain requests action
   -> Loren resolves canonical target
   -> policy requires exact owner approval
   -> approval is validated/consumed once
   -> controlled executor resolves write credential
   -> GitHub write
   -> deterministic post-write verification
   -> audit result

4. "Tại sao mày vừa làm việc đó?"
   -> reconstruct context, action request, canonical target,
      policy decision, approval, credential purpose,
      execution, verification, and outcome
```

If these four flows are not trustworthy, v0.1 is not complete.

---

# 2. v0.1 non-goals

Defer broad web research, reminders/background scheduling, Gmail/Calendar, voice, native mobile apps, Home Assistant, unrestricted shell/computer use, autonomous production deployments, multi-user architecture, multi-agent orchestration, plugin marketplace, generic framework features, dynamic multi-provider routing, standing write permissions, and broad write automation as release requirements.

---

# 3. Architecture target

```text
Owner / Web UI
      |
      v
ASP.NET Core Host
      |
      v
Context Builder -> Loren Runtime -> IBrain
      |                  |            |
      |                  |       provider adapter
      |                  |            |
      |                  +---- ActionRequest
      |                              |
      |                              v
      |                       Action Gateway
      |                      /      |       \
      |                 Policy  Approval   Audit
      |                      \      |       /
      |                       Executor Boundary
      |                              |
      |                        Secret Resolver
      |                              |
      |                        GitHub / MCP / APIs
      |                              |
      |                        Post-write verify
      |
      +--> Loren-owned canonical state
            Project / Repository
            Durable Memory + provenance
            Approval/permission state
            Audit history
```

Hard invariants:

> **The brain can request an action. Only Loren can authorize and execute it.**

> **The brain can consume prepared memory. It cannot silently promote model/external content into trusted owner truth or permission.**

> **Authentication proves owner identity. It is not write approval.**

> **A write is not successful until Loren verifies the intended external postcondition.**

---

# 4. Milestone 0 — Validate and accept ADR-002 [COMPLETE]

M0 proved provider-neutral brain/action round trip, cancellation/bounds, MCP feasibility, SQLite/EF migration feasibility, ASP.NET host feasibility, and provider/tool credential separation.

# 5. Milestone 1 — Engineering foundation [COMPLETE]

Production scaffold, .NET 10/C# 14 baseline, central packages, warnings-as-errors/analyzers, provider-neutral Core contracts, bounded AgentLoop, deterministic tests, CI gates, environment/development docs, and dependency boundaries are complete.

# 6. Milestone 2 — Walking skeleton [COMPLETE]

Trusted exact-main workflow run `33840149005` proved authenticated owner -> real brain -> Loren Action Gateway -> real GitHub read -> structured result -> final answer -> correlated audit.

# 7. Milestone 3 — Canonical state and minimal world model [COMPLETE]

- Slice 1: PR #15, merge `00fbba08587ba8275c121fd7f9532a785f55314d`, CI #99 / `33842440251` — PASS.
- Slice 2: PR #16, merge `56fd988d3b74c754604355e3c97a5d3656675bbb`, CI #108 / `33843405386`, post-merge #109 / `33843524467` — PASS.
- Slice 3 / Gate C: PR #17, ADR-003, CI #110 / `33860095412`, merge `69223e8c4923510bb26fa50f77a3c44c1683b172` — PASS.

**Gate C PASSED. M3 COMPLETE.**

---

# 8. Milestone 4 — Trusted durable memory [COMPLETE]

M4 delivers provider-independent durable memory without transcript dumping and without allowing model/external text to silently become owner truth.

## Slice summary

- PR #18 — OWNER_EXPLICIT persistence; merge `78adc287f7ae3744352b7019e3b8a838a5de499e`; final CI #117 / `33860985267`, post-merge #118 / `33861089270`.
- PR #19 — owner correction + supersession; merge `201b83eff0c6c3143856e348b4c9f029cc14a8b1`; final CI #123 / `33861630472`.
- PR #20 — authority-aware prepared memory; merge `732b85db3a799638bcd73558f98232b276f3cb5e`; final CI #131 / `33864946328`.
- PR #21 — owner forget/delete; merge `87b5a39ccae7c931de9668fed5283a4742be73f7`; final CI #137 / `33865716479`.
- PR #22 — poisoning/provenance boundary; merge `41396bf0f78b109d0af8f562039ce5f5cf1ad787`; final CI #148 / `33870438763`, post-merge #149 / `33870545850`.
- PR #23 — Windows SQLite integration cleanup hardening; merge `1cdd849126310745652d87f1d100c34aed624079`; PR CI #162 / `33893832128` and post-merge #163 / `33894104116` passed Ubuntu full gates + Windows integration; owner local Windows full integration suite also passed.

M4 proves owner memory survives restart; correction preserves append/supersede history/current-truth semantics; full correction chains can be forgotten without resurrection; prepared memory is bounded/inspectable; model/external content cannot silently become owner truth; ordinary runtime turns do not silently mutate durable memory; and memory deletion remains separate from audit retention.

**M4 COMPLETE.**

---

# 9. Gate D — Action/Credential Policy [PASSED]

**Decision:** ADR-004 — Action Approval and Credential Boundary.

Gate D locks the first write-capable policy contract before M5 executors exist.

## 9.1 Explicit action access/risk

M5 must add Loren-owned classification at least for:

```text
READ
REVERSIBLE_WRITE
EXTERNAL_WRITE
PRIVILEGED_WRITE
```

Authorization may not be inferred only from names or model text.

## 9.2 Canonical target binding

Before any write authorization, Loren resolves the request to:

```text
ProjectId
RepositoryId
provider/external locator snapshot
target ref/branch/path/base/head
whether target is default branch
normalized security-relevant parameters
```

Unknown/mismatched scope fails before credential resolution.

## 9.3 All first-version real writes require explicit owner approval

The first M5 write-capable implementation does not use standing permissions. Even reversible remote branch creation requires owner approval until the one-time path is proven.

Authentication/session identity is necessary but not sufficient.

## 9.4 Approval artifact

Approval is Loren-owned, exact-request-bound, expiring/task-bounded, and one-time.

Minimum fields/semantics:

```text
ApprovalId
authenticated principal/session binding
action identity
ProjectId + RepositoryId
normalized target/resource
security-relevant parameter digest
approved/issued timestamp
expiry/task boundary
consumption state
optional prerequisite snapshot/digest
```

Material request changes require fresh approval.

## 9.5 Non-replay

Approval is atomically consumed before the first consequential executor attempt. Consumed/expired/mismatched/unknown/revoked approval fails closed. Independent retry after ambiguous failure requires new approval.

## 9.6 Credential boundary

Write credentials:

- resolve only after policy + approval consumption;
- never enter BrainContext/model-visible action args/canonical state/memory/audit/owner-visible result;
- are referenced only by opaque write-specific purpose/ref;
- fail closed when missing/revoked;
- never silently fall back to broader credentials.

Read/write credential purposes remain logically separate.

## 9.7 Global read-only

A host-controlled fail-closed global read-only state must exist before the first write executor ships.

Absent/invalid write-enable configuration means read-only. In read-only mode no write executor runs and no write credential resolves.

## 9.8 Post-write verification + audit

Every write must verify the intended external state by an independent read before returning success. Audit reconstructs proposal -> canonical target -> policy -> approval -> credential purpose -> execution -> verification without storing secrets.

## 9.9 v0.1 write scope

Allowed after M5 foundations are green:

```text
create non-default branch
create/update file via controlled commit path on non-default branch
create commit/update ref only as required by that path
open pull request
```

Forbidden:

```text
write directly to default branch
merge PR
force push/history rewrite
delete repo/branch/data
repo admin/security changes
secret-management actions
production deploy
```

**Gate D PASSED. Gate D itself adds no external write capability.**

---

# 10. Milestone 5 — Action policy, approvals, credentials, and narrow GitHub writes [ACTIVE]

M5 objective: prove the first real external mutation can be requested by the brain but authorized, credentialed, executed, verified, and audited only by Loren.

## M5 Slice 1 — Policy/approval foundation [ACTIVE]

Deliver:

- typed action access/risk classification;
- resolved canonical write-policy context;
- normalized exact-request fingerprint;
- Loren-owned `ApprovalId` and approval artifact/store;
- one-time atomic consume + replay rejection;
- expiry/mismatch/revocation behavior;
- fail-closed global read-only control;
- deterministic tests proving model/external content cannot manufacture approval or bypass policy.

**No real GitHub mutation is enabled in Slice 1.**

Acceptance sketch:

```text
model proposes github write
 -> canonical repo resolves
 -> policy says REQUIRE_APPROVAL
 -> owner approval artifact binds exact request
 -> consume once -> authorized
 -> same approval replay -> DENY
 -> changed branch/path/content digest -> DENY
 -> global read-only -> DENY before credential resolver/executor
```

## M5 Slice 2 — Credential boundary + redaction

Deliver:

- write-specific credential resolver interface;
- host/env secret implementation for local v0.1;
- secret value only inside executor boundary;
- missing/revoked credential fail-closed;
- no fallback to broader credential;
- redaction tests covering logs/exceptions/audit/action results/brain context.

Still no broad write surface.

## M5 Slice 3 — GitHub create branch

First real write:

```text
exact owner approval
 -> policy allow
 -> consume approval
 -> resolve GitHub write credential
 -> create NON-default branch
 -> fetch branch/ref
 -> verify exact source SHA
 -> correlated audit
```

Default-branch write/force behavior remains blocked.

## M5 Slice 4 — Controlled file/commit path

Allow create/update file content only on an approved non-default branch through a controlled GitHub commit path. Approval binds repo, branch, path, content digest and relevant commit metadata. Verify resulting commit/ref/file state.

## M5 Slice 5 — Open pull request

Approval binds repo/base/head/title/body digest or other security-relevant normalized parameters. Execute then fetch PR and verify repo/base/head/state/PR identity.

## M5 Slice 6 — End-to-end authorization/audit acceptance

Prove:

- approval non-replay across runs/actions;
- read-only kill blocks writes before credential resolution;
- revoked credential blocks previously approved intent;
- write secret never reaches brain/memory/audit/result;
- hostile repository/tool text cannot expand approval;
- uncertain external result is not reported successful without verification;
- complete audit can explain why the write happened.

M5 is complete only after all slices are green on main and the narrow write flow is owner-testable.

---

# 11. Milestone 6 — Minimal daily-use UI

Required UI includes conversation, active run/tool activity, approvals, Project context, memory inspect/correct/delete, audit timeline, cancellation, and clear errors.

# 12. Milestone 7 — Recovery and state ownership proof

Logical export `format_version = 1` must restore Projects/Repositories, durable memory/provenance, permission/approval state as appropriate, retained audit, and canonical IDs into current migrated schema without provider/runtime session state. Raw credentials are never exported.

# 13. Milestone 8 — Security and reliability E2E

Cover restart continuity, aliases, memory correction/poisoning, grounded external facts, approvals/writes, prompt injection, replay, retries, stale state, provider/tool failures, credential revocation/redaction, read-only kill, cancellation, export/restore, and audit reconstruction.

# 14. v0.1 release gate

Tag `v0.1.0` only when the four required flows and memory/action/approval/recovery/security gates are proven, CI is green across required platforms, and known limitations are documented.

# 15. Current execution sequence

```text
M4 COMPLETE
 -> Gate D PASSED / ADR-004
 -> M5 Slice 1 policy + approval foundation  <- NOW
 -> M5 credential boundary
 -> M5 verified narrow GitHub writes
 -> M6 minimal daily-use UI
 -> M7 export/restore
 -> M8 adversarial E2E
 -> v0.1 release gate
```

Do not silently expand v0.1 scope. v0.2 capabilities stay deferred until the v0.1 trust boundary passes.
