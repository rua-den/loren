# ADR-002: Loren v0.1 Technology Stack

- **Status:** Proposed — M0 code/infrastructure proofs passed; live OpenAI proof blocked by missing repository `OPENAI_API_KEY` secret
- **Date:** 2026-09-03
- **Decision owners:** Loren project

## Context

ADR-001 established that Loren owns identity, canonical state, memory, policy, action authorization, and audit history. The remaining question is how to implement the first useful version without introducing a large agent framework that would duplicate Loren's core responsibilities.

The v0.1 stack should optimize for:

1. deterministic control of the model/tool loop;
2. easy implementation of the Loren Action Gateway;
3. first-class MCP support without making MCP the domain model;
4. low operational complexity for a one-owner/self-hosted system;
5. strong typing and testability;
6. isolation of unstable/provider-specific SDK surfaces behind adapters.

## Proposed decision

### Language/runtime

- **C# 14 / .NET 10 LTS**
- **ASP.NET Core** as the host

### Brain

- **OpenAI Responses API** as the initial reasoning provider
- official `openai-dotnet` SDK
- Loren-defined `IBrain` boundary around the provider

Conceptual contract:

```csharp
public interface IBrain
{
    Task<BrainTurnResult> ThinkAsync(
        BrainContext context,
        IReadOnlyList<ActionDefinition> availableActions,
        CancellationToken cancellationToken);
}
```

The provider returns text/reasoning outputs and structured action requests. It does not directly own authorization or privileged execution.

### Agent runtime

Use a **small Loren-owned agent loop**, not LangChain, Semantic Kernel, OpenClaw, Letta, or another broad orchestration framework for v0.1.

Conceptual loop:

```text
prepare context
    -> call brain
    -> receive final output OR ActionRequest
    -> validate ActionRequest
    -> Action Gateway / approval
    -> controlled executor
    -> structured ActionResult
    -> feed result back to brain
    -> repeat within bounded limits
```

The loop must have hard limits for turns/tool calls/cancellation and must be testable with a fake `IBrain`.

### MCP

- use the official **Model Context Protocol C# SDK** for MCP client integration where useful;
- prefer the smallest package surface needed (`ModelContextProtocol.Core` when sufficient);
- normalize MCP tools into Loren `ActionDefinition` / `ActionRequest` / `ActionResult` contracts;
- do not expose privileged remote MCP tools directly to the model through provider-managed remote-MCP execution when that would bypass Loren's Action Gateway.

MCP is an integration transport. Loren may also use direct vendor APIs for capabilities where that is simpler or safer.

### Persistence

- **SQLite** for canonical v0.1 state
- **Entity Framework Core** for schema/migrations

Initial storage needs are single-owner, low-throughput, and local/self-hosted. A server database is not justified until real concurrency/scale requires it.

### UI

- **Blazor Web App** hosted with ASP.NET Core for v0.1
- prioritize a single deployable stack and low moving-part count over frontend specialization
- UI remains replaceable; no domain or runtime logic belongs in Blazor components

### Testing

- xUnit for unit/integration tests
- fake `IBrain`, fake executors, and deterministic policy tests
- real-provider/MCP/GitHub tests separated from deterministic core tests

## Important SDK stability note

As of the 2026-09-03 planning snapshot:

- .NET 10 is an LTS release;
- the official MCP C# SDK is a Tier 1 SDK;
- OpenAI's .NET Responses client supports Responses/function-calling and MCP-related APIs, but parts of the Responses surface are still marked experimental in the SDK.

Therefore v0.1 must pin exact package versions and keep provider/MCP SDK types behind adapters. No experimental/provider-specific type may become a Loren domain type.

## Milestone 0 validation before acceptance

Before changing this ADR to **Accepted**, create disposable technical spikes that prove:

### Brain spike

- call the selected OpenAI model through Responses API;
- expose a Loren-defined function/action schema;
- receive a structured function/action request;
- execute a fake action through Loren code;
- return the structured result and receive a final response;
- cancel a run;
- enforce a bounded tool/turn limit;
- verify no API key/tool credential appears in model-visible context or logs.

### MCP spike

- connect to one harmless MCP server using the official C# SDK;
- enumerate/normalize tool metadata into Loren action definitions;
- invoke one read-only MCP tool through Loren's executor path;
- prove MCP SDK types do not cross into `Loren.Core`.

### Persistence spike

- create SQLite database through EF Core migration;
- persist and reload a minimal Owner/Project/Audit record;
- perform an export/restore smoke test at the storage boundary.

### Web-host spike

- run ASP.NET Core + minimal Blazor page;
- execute one fake brain request end to end through DI/cancellation/logging.

## M0 evidence — 2026-09-03

The disposable spikes live under `spikes/adr-002/`. They are evidence for the stack decision, not production architecture.

### Brain — wired and guarded; credential-backed execution still open

Validated:

- target framework `net10.0` / C# 14;
- official `OpenAI` .NET package pinned to `2.12.0`;
- Responses function schema compiles against the pinned SDK;
- provider function arguments are intercepted as SDK `BinaryData` and parsed/validated inside Loren-owned code;
- only a registered `get_project_status` action can cross the spike `ActionGateway`;
- fake executor returns structured JSON and never receives the OpenAI API key;
- the Responses loop uses `CreateResponseAsync` and passes one `CancellationToken` through every provider request;
- `Ctrl+C` and a wall-clock timeout cancel the same token used by the provider call;
- a dedicated live cancellation mode arms cancellation immediately before the provider await and reports PASS only if cancellation is observed there;
- hard six-turn bound exists independently of the wall-clock timeout;
- the spike cannot report PASS unless a model-requested action actually crossed the Loren gateway;
- GitHub Actions compiles the async/cancellation path on .NET 10 with zero build errors;
- ordinary PR validation never receives the OpenAI API key.

Trusted live-trigger boundary validated:

- PR #5 added a connector-safe one-shot branch trigger in addition to manual `workflow_dispatch`;
- manual dispatch must run from `main`;
- connector-triggered live validation is restricted to `spike/adr-002-live-proof-run`;
- before any secret-backed step, CI fetches `origin/main` and requires the trigger SHA to equal current `main` exactly;
- run #53 executed this guard successfully from commit `315d21661aae4d9c8da30617f91a93e8b31d85ff`.

Run #53 then established the current operational blocker:

```text
brain spike build                  PASS
trusted trigger / SHA guard        PASS
secret detection                   PASS
OPENAI_API_KEY present?            NO
fail-closed secret requirement     EXPECTED FAILURE
live OpenAI round trip             NOT RUN
live cancellation proof            NOT RUN
```

The workflow log explicitly reported:

```text
OPENAI_API_KEY repository secret is required to close ADR-002 Gate B.
```

Still required before ADR acceptance:

- configure `OPENAI_API_KEY` as a GitHub Actions repository secret, or run the spike locally with the key supplied through the environment;
- execute the real OpenAI Responses round trip;
- observe a real model-requested action -> Loren gateway -> structured result -> final model response;
- execute the live cancellation mode and confirm the provider await terminates through Loren's cancellation token;
- confirm from live logs that no provider/tool secret is emitted.

### MCP — passed

Validated in CI:

- official `ModelContextProtocol` C# package pinned to `2.2.0`;
- reference server pinned to `@modelcontextprotocol/server-everything@2026.8.31`;
- stdio MCP connection established successfully;
- 13 tool definitions were enumerated and normalized into a Loren-owned `LorenActionDefinition` shape;
- only the explicitly allow-listed read-only `echo` action was executable through the spike gateway;
- `echo` executed successfully and returned structured MCP content;
- MCP SDK result types remain inside the adapter/spike boundary rather than defining Loren domain identity.

### Persistence — passed

Validated in CI with `Microsoft.EntityFrameworkCore.Sqlite` and `Microsoft.EntityFrameworkCore.Design` pinned to `10.0.11`:

```text
EF migration generation
-> migrate SQLite database
-> persist Owner + Project + Audit sample state
-> export canonical state to JSON
-> EnsureDeleted wipe
-> migrate fresh database
-> restore export
-> reload and verify Project/Audit state
```

The complete path passed. This is sufficient evidence that SQLite + EF Core can support the v0.1 canonical-state and disaster-recovery proof. It does not yet define the final production schema or retention policy.

### ASP.NET Core / Blazor host — passed

Validated in CI:

- ASP.NET Core / Blazor project builds on .NET 10;
- application boots successfully;
- `/health` responds successfully;
- Blazor root page renders;
- a fake brain is resolved through dependency injection;
- request cancellation token and logging are available at the brain boundary;
- `/brain?input=hello` returns the fake brain result end to end.

### CI evidence

- PR #1 established the OpenAI .NET function-calling compile boundary and mandatory ActionGateway crossing before PASS.
- PR #2 established MCP, persistence, and web-host execution; its final validation run passed every M0 no-secret step.
- PR #3 moved the brain loop to async provider calls and added cancellation propagation.
- PR #4 automated the normal live tool round trip and provider-await cancellation proof while keeping PR CI secret-free.
- PR #5 added a connector-safe trusted trigger with exact-main SHA verification before secret access.
- trusted run #53 proved the trigger/security guard and fail-closed missing-secret behavior; no live provider request was made because the repository secret is absent.

## Current M0 decision

All code and no-secret infrastructure areas are technically proven enough to attempt the final live provider validation:

```text
MCP client / gateway        PASS
SQLite + EF Core            PASS
ASP.NET Core + Blazor       PASS
OpenAI brain wiring         PASS
trusted live trigger        PASS
repository OPENAI_API_KEY   MISSING / BLOCKER
OpenAI live round trip      OPEN
OpenAI live cancellation    OPEN
```

ADR-002 therefore remains **Proposed**. Do not scaffold production Loren until the exact pinned SDK/model path completes the live normal and cancellation proofs.

The current blocker is credential configuration, not a discovered architectural failure.

## Acceptance criteria for ADR-002

Accept this stack if all of the following are true:

- the custom loop is small and understandable rather than becoming a framework project;
- OpenAI tool/function calls can be intercepted before every side effect;
- provider credentials and tool credentials can remain separated;
- official MCP client integration works behind Loren abstractions;
- SQLite/EF Core satisfies canonical-state/recovery requirements for v0.1;
- the full host can run locally with a small operational footprint;
- no experimental SDK surface leaks into Loren.Core.

If one area fails, replace only that adapter/component rather than reopening ADR-001.

## Proposed solution layout

```text
src/
├── Loren.Api/              # ASP.NET Core host/API
├── Loren.Core/             # domain, memory contracts, policy/action contracts
├── Loren.Runtime/          # context builder + thin agent loop
├── Loren.Brain.OpenAI/     # OpenAI Responses adapter
├── Loren.Tools/            # action registry/executors
├── Loren.Tools.Mcp/        # MCP adapter
├── Loren.Tools.GitHub/     # GitHub capability
├── Loren.Infrastructure/   # EF Core, SQLite, secrets, audit persistence
└── Loren.Web/              # Blazor UI

tests/
├── Loren.Core.Tests/
├── Loren.Runtime.Tests/
├── Loren.IntegrationTests/
└── Loren.E2E.Tests/
```

The exact project count may be reduced during scaffolding if this creates unnecessary ceremony. Dependency direction matters more than folder count.

## Sources checked for this planning snapshot

- Microsoft .NET release/support documentation
- official `openai/openai-dotnet` repository and Responses examples
- official Model Context Protocol C# SDK and SDK-tier documentation
- npm package metadata for the pinned MCP reference server

Re-check package/API versions when production scaffolding starts; the repository should retain the exact versions actually validated.
